[package]
name = "tauri-plugin-gallery"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
links = "tauri-plugin-gallery"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = { version = "2" }
