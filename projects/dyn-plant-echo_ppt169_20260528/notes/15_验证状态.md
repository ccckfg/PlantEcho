[过渡] 让我详细展示验证状态。

后端：25个测试用例全部通过，覆盖设备、植物、聊天、记忆、主动发言等模块。

检索评估：120条记忆 + 48个查询，Hybrid + Rerank 组合全部满分。

Docker 部署：本地镜像构建、容器验证、Docker Hub 推送全部完成。/health 端点、鉴权、设备认领、数据上报等全流程验证通过。

[停顿] 桌面客户端：Tauri v2.11.2 + Rust release 已产出 msi 和 nsis 安装包。

ESP32 真实硬件：OLED、SHT40、GY-302、土壤 ADC 实测可用；HTTP 上传、MQTT 上报、断线重连、SoftAP 配网、OTA、设备密钥自动接收全部验证通过。

要点：① 后端测试 ② 检索评估 ③ Docker 部署 ④ 桌面端 ⑤ ESP32 硬件
时长：2分钟