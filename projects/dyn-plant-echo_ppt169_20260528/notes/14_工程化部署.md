[过渡] 说完技术亮点，看看工程化。

Docker 方面，我们使用多阶段构建，镜像已经推送到 Docker Hub（ccckfg/dyn:latest）。Docker Compose 支持单实例灰度部署，暴露 HTTP 8787 和 MQTT 1883 两个端口。

Monorepo 方面，使用 npm workspaces 管理三个包：packages/shared 共享 schema、apps/server 核心后端、apps/desktop 桌面端。

[停顿] 测试方面，25个服务端测试全部通过，4个 smoke 测试全部绿色，检索评估 100% 准确。

要点：① Docker 容器化 ② Monorepo 架构 ③ 测试覆盖
时长：2分钟